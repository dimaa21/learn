// JS + HTML
/*
// вся структура html елемента, доступ до
// console.log(document.documentElement);

// вибрати елемент body
// console.log(document.body);

// вибрати елемент head
// console.log(document.head);

// вибрати перший елемент тегу body
// console.log(document.body.childNodes);

// for(var i = 0; i < document.body.childNodes.length; i++) {
//    console.log(document.body.childNodes[i]);
// }

// вибрати певний елемент по id
// let content = document.getElementById("content");

// вибираємо всі елементи з тегом
// let elements = content.getElementsByTagName("p");
// console.log(elements);

// виводимо всі елементи з тегом p
// for(var i = 0; i < elements.length; i++) {
//     console.log(elements);
// }

// вибір елементів з атрибутом
let el = document.getElementsByName("fname")[0].tagName;
console.log(el);

// вибірка елементів за класом
let allClasses = document.getElementsByClassName('some');
console.log(allClasses);

// вибірка елементів, як в CSS
let elements = document.querySelectorAll('ul.test > li');
console.log(elements);

// вибираємо найближчий тег li до id #span-text
let  ulItems = document.querySelectorAll('.span-text')[0];

// змінюємо значення елемента з класом .some-text
let parentLi = ulItems.closest(".some-text");
console.log(parentLi.innerHTML);

let input = document.querySelector("input[type='text']");
input.value = 'New';

// перевіряємо, чи є атрибут
console.log(input.hasAttributes('type'));
// дозволяє отримати значення артибута
console.log(input.getAttribute('type'));
// встановлюємо атрибут з певним значенням
input.setAttribute('data-toggle', 'some value');

// допусуємо до html структури, якийсь текст
document.write('HTML');

// JS + CSS
input.style.backgroundColor = "red";
input.style.color = 'white';
input.style.borderRadius = '5px';
input.style.padding = '15px 10px'; 


let selectDiv = document.querySelector('div');
console.log(selectDiv);

let selectUl = document.querySelector('ul');
console.log(selectUl);

let selectSecondLi = document.querySelector('li:nth-child(2)');
console.log(selectSecondLi);

var elements = document.querySelectorAll('.red');

// Встановлюємо фоновий колір для всіх елементів однією командою
elements.forEach(function(element) {
    element.style.backgroundColor = 'red'; // Наприклад, 'red', '#00ff00', 'rgb(255, 0, 0)' і т. д.
}); */






